server:
  port: 3000
spring:
  application:
    name: cloud-guli-yhq
  cloud:
    nacos:
      server-addr: localhost:9948
      discovery:
        ip: 127.0.0.1
      config:
        file-extension: yaml
        namespace: 10086003
        extension-configs:
          - data-id: mybatis-plus.yaml
            group: dev
            refresh: true

          - data-id: datasource.yaml
            group: dev
            refresh: true
