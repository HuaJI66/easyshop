spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:9948
        file-extension: yaml
        namespace: 10086001
        extension-configs:
          -
            data-id: mybatis-plus.yaml
            group: dev
            refresh: true

          -
            data-id: datasource.yaml
            group: dev
            refresh: true

          -
            data-id: registry.yaml
            group: dev
            refresh: true


