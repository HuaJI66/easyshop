spring:
  cloud:
    nacos:
      discovery:
#        ip: 10.2.11.11
        ip: 127.0.0.1
#        network-interface: eth0
      config:
        server-addr: localhost:9948
        namespace: 10086008
        file-extension: yaml
        extension-configs:
          -
            data-id: registry.yaml
            group: dev
            refresh: true
          -
            data-id: oss.yaml
            group: dev
            refresh: true
