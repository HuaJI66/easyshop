spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:9948
        namespace: 10086008
        file-extension: yaml
        extension-configs:
          -
            data-id: registry.yaml
            group: dev
            refresh: true
          -
            data-id: oss.yaml
            group: dev
            refresh: true

