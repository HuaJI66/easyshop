spring:
  application:
    name: cloud-guli-fast
  cloud:
    nacos:
      discovery:
#        ip: 10.2.11.11
        ip: 127.0.0.1
        server-addr: localhost:9948
      config:
        server-addr: localhost:9948
        namespace: 10086007
        file-extension: yaml

